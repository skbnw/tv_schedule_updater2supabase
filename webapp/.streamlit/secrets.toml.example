# これを .streamlit/secrets.toml にコピーして値を設定（.gitignore 済み）。
# 接続は Supabase API 経由（DBパスワード不要）。
#   SUPABASE_URL        : Project Settings > API > Project URL
#   SUPABASE_SECRET_KEY : Project Settings > API keys > secret key（sb_secret_...）
#   ※ secret キーはサーバ用途のみ。クライアント公開しないこと。露出時はローテーション。
SUPABASE_URL = "https://lpwqrnmsmscabhyexzxm.supabase.co"
SUPABASE_SECRET_KEY = "sb_secret_..."
